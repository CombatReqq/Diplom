<Application x:Class="PP.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PP"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <Style x:Key="mainBtn" TargetType="Button">
            <Setter Property="Background" Value="#413ef7"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Verdana"/>
            <Setter Property="BorderBrush" Value="DarkBlue"/>
        </Style>
                <Style x:Key="LightSTCpannel" TargetType="StackPanel">
            <Setter Property="Background" Value="#9dc3fc"/>
            </Style>
      
                <Style x:Key="mainCmb" TargetType="ComboBox">
                <Setter Property="Background" Value="#FFFFCC99"/>
                <Setter Property="BorderBrush" Value="#FFFFCC99"/>
            </Style>
    </Application.Resources>
</Application>
